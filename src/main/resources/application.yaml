server:
  port: 5000

localeTag: en-UK

telegram-bot:
  botUsername: ${USERNAME}
  botToken: ${TOKEN}
  botPath: ${WEB_HOOK_PATH}

  google:
    calendar:
      credentials:
        path: "classpath:credentials.json"
